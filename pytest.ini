[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*

python_functions = test_*
addopts = -v --tb=short --strict-markers --cov=src --cov-report=term-missing --cov-branch -q -p no:warnings
markers =
    slow: marks tests as slow
    integration: marks tests as integration tests
